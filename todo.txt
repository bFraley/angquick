todo.txt

-Comment lib file
-README
-tests
-A ton more error / exception handling
-More/next Angular generation features

- Auto template function definitions
- Dont' use explicit 'clients dir',
  have user define this.
- Option to create a services file.

function someService() {
    return {

    };
}

export default someService;

-N register with flashes.module

const FlashesModule = angular.module('flashes', [])
    .factory('flashesService', flashesService)
    .component('flashes, flashesService')

-N inject into controller

function FlashesController(flashesService) {
    const ctrl = this;
    ctrl.flashes = flashesService.flashes
}

-Choose auto template options.


FILE: -controller.js

function SOMENAME.Controller

FILE: .html

- page.html

<name-item/>

FILE: 

-callbacks that bind to a component return one value, an object